import { StyleSheet, Text, View } from 'react-native';

export default function ObjectRegister() {
	return (
		<View>
			<Text>ObjectRegister</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
