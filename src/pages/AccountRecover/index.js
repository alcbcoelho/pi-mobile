import { StyleSheet, Text, View } from 'react-native';

export default function AccountRecover() {
	return (
		<View>
			<Text>AccountRecover</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
